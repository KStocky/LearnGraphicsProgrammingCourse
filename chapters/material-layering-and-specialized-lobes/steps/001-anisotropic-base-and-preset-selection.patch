diff --git a/chapters/material-layering-and-specialized-lobes/Starter/MaterialLab.hlsl b/chapters/material-layering-and-specialized-lobes/Starter/MaterialLab.hlsl
index 5e63b03..b1fec9f 100644
--- a/chapters/material-layering-and-specialized-lobes/Starter/MaterialLab.hlsl
+++ b/chapters/material-layering-and-specialized-lobes/Starter/MaterialLab.hlsl
@@ -11,13 +11,29 @@ SurfaceVertex SphereVS(MeshVertex input)
     return MaterialLabVertex(input);
 }
 
-float4 BaselinePS(SurfaceVertex input) : SV_Target
+float4 LightingPS(SurfaceVertex input) : SV_Target
 {
     float3 normal = normalize(input.worldNormal);
+    float3 tangentHint = input.worldTangent.xyz;
     float3 viewDirection = normalize(gLab.cameraPosition - input.worldPosition);
     float3 lightDirection = normalize(gLab.directionToLight);
-    float3 radiance = ShadeBaselineRadiance(normal, input.worldTangent.xyz, viewDirection, lightDirection);
-    return float4(radiance, 1.0f);
+
+    if (gLab.preset == PresetBaseline)
+    {
+        float3 baseline = ShadeBaselineRadiance(normal, tangentHint, viewDirection, lightDirection);
+        return float4(baseline, 1.0f);
+    }
+
+    // Non-baseline presets shade the anisotropic base lobe directly in tangent
+    // space. Clearcoat and emission are layered on in later steps.
+    ShadingBasis basis = MakeShadingBasis(normal, tangentHint);
+    float3 localView = ToTangentSpace(basis, viewDirection);
+    float3 localLight = ToTangentSpace(basis, lightDirection);
+    float nDotL = saturate(localLight.z);
+    float2 alpha = MapAnisotropicRoughness(gLab.perceptualRoughness, gLab.anisotropy);
+    float3 brdf = EvaluateBaseLobe(localView, localLight, alpha, gLab.baseColor, gLab.baseDielectricF0, gLab.metallic);
+    float3 irradiance = gLab.lightColor * gLab.lightIntensity * nDotL;
+    return float4(brdf * irradiance, 1.0f);
 }
 
 FullscreenVertex FullscreenVS(uint vertexId : SV_VertexID)
diff --git a/chapters/material-layering-and-specialized-lobes/Starter/Renderer.cpp b/chapters/material-layering-and-specialized-lobes/Starter/Renderer.cpp
index a3aef2d..f1a9788 100644
--- a/chapters/material-layering-and-specialized-lobes/Starter/Renderer.cpp
+++ b/chapters/material-layering-and-specialized-lobes/Starter/Renderer.cpp
@@ -76,7 +76,7 @@ lgp::framework::Status Renderer::CreateShaders()
     {
         return status;
     }
-    if (auto status = gpu::CompileShaderEntry(compiler, options, L"BaselinePS", L"ps_6_0", baselinePixelShader_);
+    if (auto status = gpu::CompileShaderEntry(compiler, options, L"LightingPS", L"ps_6_0", lightingPixelShader_);
         !status)
     {
         return status;
@@ -214,7 +214,7 @@ lgp::framework::Status Renderer::CreatePipelineStates()
     D3D12_GRAPHICS_PIPELINE_STATE_DESC sphereDescription{};
     sphereDescription.pRootSignature = lightingRootSignature_.Get();
     sphereDescription.VS = sphereVertexShader_.Bytecode();
-    sphereDescription.PS = baselinePixelShader_.Bytecode();
+    sphereDescription.PS = lightingPixelShader_.Bytecode();
     sphereDescription.BlendState = blend;
     sphereDescription.SampleMask = UINT_MAX;
     sphereDescription.RasterizerState = rasterizer;
@@ -230,7 +230,7 @@ lgp::framework::Status Renderer::CreatePipelineStates()
         FAILED(result))
     {
         return std::unexpected(lgp::framework::MakeHResultError("ID3D12Device::CreateGraphicsPipelineState", result,
-                                                                "Failed to create the baseline sphere pipeline."));
+                                                                "Failed to create the sphere pipeline."));
     }
 
     D3D12_GRAPHICS_PIPELINE_STATE_DESC displayDescription{};
@@ -392,14 +392,11 @@ lgp::framework::Status Renderer::CreateSizeDependentTargets(lgp::framework::Exte
 
 gpu::LabConfiguration Renderer::ActiveConfiguration() const noexcept
 {
-    // The Starter only shades the isotropic baseline, so it always renders the
-    // baseline preset. Headless callers may override the material/light/exposure.
-    gpu::LabConfiguration configuration =
-        hasHeadlessConfiguration_ ? headlessConfiguration_ : gpu::BaselineConfiguration();
-    configuration.preset = gpu::ScenePreset::Baseline;
-    configuration.outputView = gpu::OutputView::Final;
-    configuration.geometry = gpu::SceneGeometry::Sphere;
-    return configuration;
+    if (hasHeadlessConfiguration_)
+    {
+        return headlessConfiguration_;
+    }
+    return gpu::ConfigurationForPreset(interactivePreset_);
 }
 
 gpu::CameraMatrices Renderer::ActiveCamera(lgp::framework::Extent2D size) const noexcept
@@ -470,17 +467,32 @@ lgp::framework::Status Renderer::OnResize(lgp::framework::DeviceResources &, lgp
     return CreateSizeDependentTargets(drawableSize);
 }
 
+void Renderer::ApplyInteractiveControls(lgp::framework::UpdateContext const &context)
+{
+    lgp::framework::InputState const &input = context.input;
+
+    if (input.WasKeyPressed('1'))
+    {
+        interactivePreset_ = gpu::ScenePreset::Baseline;
+    }
+    if (input.WasKeyPressed('2'))
+    {
+        interactivePreset_ = gpu::ScenePreset::Anisotropic;
+    }
+
+    if (input.mouse.IsButtonDown(lgp::framework::MouseButton::Right))
+    {
+        orbitAzimuth_ += static_cast<float>(input.mouse.deltaX) * 0.006F;
+        orbitElevation_ = std::clamp(orbitElevation_ - (static_cast<float>(input.mouse.deltaY) * 0.006F), -1.2F, 1.2F);
+    }
+    orbitRadius_ = std::clamp(orbitRadius_ - input.mouse.wheelDelta, 1.8F, 8.0F);
+}
+
 lgp::framework::Status Renderer::Update(lgp::framework::UpdateContext const &context)
 {
     if (!headless_)
     {
-        if (context.input.mouse.IsButtonDown(lgp::framework::MouseButton::Right))
-        {
-            orbitAzimuth_ += static_cast<float>(context.input.mouse.deltaX) * 0.006F;
-            orbitElevation_ =
-                std::clamp(orbitElevation_ - (static_cast<float>(context.input.mouse.deltaY) * 0.006F), -1.2F, 1.2F);
-        }
-        orbitRadius_ = std::clamp(orbitRadius_ - context.input.mouse.wheelDelta, 1.8F, 8.0F);
+        ApplyInteractiveControls(context);
     }
     return {};
 }
diff --git a/chapters/material-layering-and-specialized-lobes/Starter/Renderer.hpp b/chapters/material-layering-and-specialized-lobes/Starter/Renderer.hpp
index 887de07..e175b17 100644
--- a/chapters/material-layering-and-specialized-lobes/Starter/Renderer.hpp
+++ b/chapters/material-layering-and-specialized-lobes/Starter/Renderer.hpp
@@ -45,12 +45,13 @@ class Renderer final : public lgp::framework::IChapterRenderer
     [[nodiscard]] lgp::framework::Status CreateSizeDependentTargets(lgp::framework::Extent2D size);
     [[nodiscard]] gpu::LabConfiguration ActiveConfiguration() const noexcept;
     [[nodiscard]] gpu::CameraMatrices ActiveCamera(lgp::framework::Extent2D size) const noexcept;
+    void ApplyInteractiveControls(lgp::framework::UpdateContext const &context);
 
     lgp::framework::DeviceResources *deviceResources_{};
     bool headless_{};
 
     lgp::framework::CompiledShader sphereVertexShader_{};
-    lgp::framework::CompiledShader baselinePixelShader_{};
+    lgp::framework::CompiledShader lightingPixelShader_{};
     lgp::framework::CompiledShader fullscreenVertexShader_{};
     lgp::framework::CompiledShader displayPixelShader_{};
 
@@ -78,6 +79,7 @@ class Renderer final : public lgp::framework::IChapterRenderer
     gpu::LabConfiguration headlessConfiguration_{gpu::BaselineConfiguration()};
     bool hasHeadlessConfiguration_{};
 
+    gpu::ScenePreset interactivePreset_{gpu::ScenePreset::Baseline};
     float orbitAzimuth_{0.0F};
     float orbitElevation_{0.25F};
     float orbitRadius_{3.1F};
